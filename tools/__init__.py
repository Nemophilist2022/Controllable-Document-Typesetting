"""Project-level utilities (not part of the agent package).

This is intentionally separate from ``thesis_agent.tools`` (which holds
agent-managed Tools). Items here are CLI-style helpers for developers and
CI, e.g. semantic docx comparison used by regression tests.
"""
